export default function BlogDetailPage({ params }) {
    const { id } = params;

    return (
        <div className="w-5xl m-auto my-10">
            <h1>Cái này đang xây ^.^</h1>
            {/* Fetch blog detail by id ở đây */}
        </div>
    );
}
